name: Development overview (Pages)

on:
  push:
    branches: [main]
    paths:
      - "docs/development-overview.md"
      - "data/development-overview/**"
      - "scripts/gen-development-overview.sh"
      - "scripts/refresh-development-overview.sh"
      - "scripts/development-overview-live.js"
      - ".github/workflows/pages.yml"
      - ".github/workflows/refresh-development-overview.yml"
  pull_request:
    paths:
      - "docs/development-overview.md"
      - "data/development-overview/**"
      - "scripts/gen-development-overview.sh"
      - "scripts/refresh-development-overview.sh"
      - "scripts/development-overview-live.js"
      - ".github/workflows/pages.yml"
      - ".github/workflows/refresh-development-overview.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install build deps
        run: python3 -m pip install markdown

      - name: Refresh live status (PR builds use committed JSON)
        if: github.event_name == 'pull_request'
        run: test -f data/development-overview/status.json

      - name: Refresh live status (main / dispatch)
        if: github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x scripts/refresh-development-overview.sh
          ./scripts/refresh-development-overview.sh

      - name: Generate static site
        run: |
          chmod +x scripts/gen-development-overview.sh
          ./scripts/gen-development-overview.sh

      - name: Verify output
        run: test -f site/development-overview/index.html

      - uses: actions/upload-pages-artifact@v3
        if: github.event_name != 'pull_request'
        with:
          path: site

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
