# Optional policy hints (one name per line). LoC on the development overview
# counts `.li` source across ALL org repos (GitHub + GitLab group list via API).
# This file is a fallback census only when those APIs are unavailable.
# CI policy: every repo should have .github/workflows/ci.yml
lic
li-language
lip
lit
lis
benchmarks
roadmap
lic-docs
proof-library
li-net
li-httpd
li-std-core
li-std-math
li-demo
